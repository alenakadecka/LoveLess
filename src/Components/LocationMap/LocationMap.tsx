import { MapItem } from './MapItem';

interface LocationMapsProps {
  items: MapItem[];
}

export function LocationMap(props: LocationMapsProps) {
  //komponenta obsahuje logiku pre mapu

  return <div></div>;
}
